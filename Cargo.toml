[package]
name = "calx"
version = "0.0.1"
authors = ["Risto Saarelma <risto.saarelma@iki.fi>"]
description = "Rust game development helper library collection"
keywords = ["gamedev"]
license = "MIT"

# Doesn't actually do anything, all the stuff is in the sub-projects.
[lib]
name = "calx"

[dev-dependencies.calx_util]
path = "util"

[dev-dependencies.calx_backend]
path = "backend"
