Rust multimedia programming scratch space.

Init the submodules
$ ./init-submodules.sh

To build, you need tup from http://gittup.org/tup/:
$ tup

To activate a variant build, eg. release build, do
$ tup variant configs/release.config
